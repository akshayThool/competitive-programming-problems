#ifndef REVERSE_BITS_H
#define REVERSE_BITS_H
#include<chrono>

unsigned long long reverseBitsBruteForce(unsigned long long);
unsigned long long reverseBitsCacheMethod(unsigned long long);

#endif // REVERSE_BITS_H
