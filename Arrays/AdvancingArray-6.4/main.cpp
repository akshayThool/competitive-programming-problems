#include "AdvancingArray.h"

int main(){
    //Todo
    vector<int> A = {3, 2, 0, 1, 0, 1};
    if(isSolutionPossible(A)){
        cout<<"Solution Exists\n";
    } else {
        cout<<"No Possible\n";
    }
}
