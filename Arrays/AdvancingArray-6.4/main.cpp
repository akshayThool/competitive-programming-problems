#include "AdvancingArray.h"

int main(){
    //Todo
    vector<int> A = {3, 2, 0, 1, 1, 1};
    if(canReachEnd(A)){
        cout<<"Solution Exists\n";
    } else {
        cout<<"No Possible\n";
    }
}
